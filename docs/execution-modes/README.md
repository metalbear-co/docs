# Execution Modes

This section covers mirrord's alternative execution modes. Targetless mode lets you run mirrord without targeting a specific workload, while local container mode runs your application inside a container locally with mirrord's network and filesystem features.
