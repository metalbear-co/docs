# Queues & Data

This section covers mirrord for Teams features for working with message queues and databases. Queue splitting lets multiple developers consume messages from the same queue independently, while DB branching provides isolated database branches for safe local development.
